# GitHub data source for /projects/ (fetched at hugo build via API)
username = "user27c"
excludeForks = true
excludeRepos = ["22-7-co.github.io", "sre-portfolio"]
maxRepos = 50
sortBy = "updated" # updated | stars
